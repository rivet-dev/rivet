apiVersion: v1
kind: Service
metadata:
  name: pgbouncer
  namespace: rivet-engine
  labels:
    app: pgbouncer
    service: engine
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: pgbouncer
  selector:
    app: pgbouncer
