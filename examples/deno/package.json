{
	"//": "This package.json is required, once is released with updated engine-runner packages, it can be converted to deno.json",
	"name": "example-deno",
	"version": "2.0.21",
	"private": true,
	"type": "module",
	"scripts": {
		"dev": "deno run --allow-all src/server.ts",
		"check-types": "command -v deno >/dev/null 2>&1 && deno task check-types || echo 'Skipping deno check-types (deno not installed)'",
		"connect": "deno run --allow-all scripts/connect.ts",
		"build": "tsc"
	},
	"devDependencies": {
		"@types/deno": "^2.3.0",
		"@types/node": "^24.5.2"
	},
	"stableVersion": "0.8.0",
	"dependencies": {
		"hono": "4.9.8",
		"rivetkit": "*"
	},
	"template": {
		"technologies": [
			"deno",
			"typescript"
		],
		"tags": [],
		"noFrontend": true
	},
	"license": "MIT"
}
