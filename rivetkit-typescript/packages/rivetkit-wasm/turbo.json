{
	"$schema": "https://turbo.build/schema.json",
	"extends": ["//"],
	"tasks": {
		"build": {
			// Cargo tracks shared Rust inputs outside this package; Turbo's default
			// task neither tracks those inputs nor restores the generated pkg.
			"cache": false
		}
	}
}
