# @rivetkit/cli

Rivet Cloud CLI distributed over npm.

```sh
npx @rivetkit/cli dev
npx @rivetkit/cli dev --provider cloudflare
npx @rivetkit/cli dev --provider supabase -- --env-file .env.local
npx @rivetkit/cli deploy --token cloud_api_xxxxx
npx @rivetkit/cli setup-ci
```
