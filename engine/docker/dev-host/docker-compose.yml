services:
  clickhouse:
    restart: unless-stopped
    image: clickhouse/clickhouse-server:25.1.5
    volumes:
      - clickhouse-data:/var/lib/clickhouse
      - ./clickhouse/config.xml:/etc/clickhouse-server/config.d/config.xml
      - ./clickhouse/users.xml:/etc/clickhouse-server/users.d/users.xml
      - ./clickhouse/client-config.xml:/etc/clickhouse-client/config.xml
      - ./clickhouse/init:/docker-entrypoint-initdb.d
    environment:
      - CLICKHOUSE_ALWAYS_RUN_INITDB_SCRIPTS=true
      - CLICKHOUSE_USER=default
      - CLICKHOUSE_PASSWORD=default
      - CLICKHOUSE_HTTP_PORT=9300
      - CLICKHOUSE_TCP_PORT=9301
    healthcheck:
      test:
        - CMD
        - clickhouse-client
        - '--host'
        - 127.0.0.1
        - '--port'
        - '9301'
        - '--user'
        - system
        - '--password'
        - default
        - '--query'
        - SELECT 1
      interval: 2s
      timeout: 10s
      retries: 10
    network_mode: host
  prometheus:
    restart: unless-stopped
    image: prom/prometheus:latest
    volumes:
      - prometheus-data:/prometheus
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.console.libraries=/usr/share/prometheus/console_libraries'
      - '--web.console.templates=/usr/share/prometheus/consoles'
      - '--web.enable-remote-write-receiver'
    healthcheck:
      test:
        - CMD
        - wget
        - '--spider'
        - '-q'
        - http://127.0.0.1:9090/-/healthy
      interval: 5s
      timeout: 3s
      retries: 3
    network_mode: host
  grafana:
    image: grafana/grafana:11.5.2
    volumes:
      - grafana-data:/var/lib/grafana
      - ./grafana/grafana.ini:/etc/grafana/grafana.ini
      - ./grafana/provisioning:/etc/grafana/provisioning
      - ./grafana/dashboards:/var/lib/grafana/dashboards
    environment:
      - GF_INSTALL_PLUGINS=grafana-clickhouse-datasource
    depends_on:
      clickhouse:
        condition: service_healthy
      prometheus:
        condition: service_healthy
    network_mode: host
  postgres:
    restart: unless-stopped
    image: postgres:17-alpine
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=postgres
    volumes:
      - ./postgres/init-db.sh:/docker-entrypoint-initdb.d/init-db.sh
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U postgres -d postgres
      interval: 2s
      timeout: 10s
      retries: 10
    network_mode: host
  rivet-shell:
    build:
      context: ../../..
      dockerfile: engine/docker/universal/Dockerfile
      target: engine-full
      args:
        BUILD_FRONTEND: 'true'
    platform: linux/amd64
    restart: unless-stopped
    entrypoint: sleep
    command: infinity
    stop_grace_period: 0s
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./rivet-engine/config.jsonc:/etc/rivet/config.jsonc:ro
    network_mode: host
  vector-server:
    restart: unless-stopped
    image: timberio/vector:0.48.0-distroless-static
    command: '-C /etc/vector'
    volumes:
      - vector-server-data-default:/var/lib/vector
      - ./vector-server:/etc/vector
    depends_on:
      clickhouse:
        condition: service_healthy
    network_mode: host
  vector-client:
    restart: unless-stopped
    image: timberio/vector:0.48.0-distroless-static
    command: '-C /etc/vector'
    volumes:
      - vector-client-data-default:/var/lib/vector
      - ./vector-client:/etc/vector
    depends_on:
      vector-server:
        condition: service_started
    network_mode: host
  otel-collector:
    image: otel/opentelemetry-collector-contrib:latest
    restart: unless-stopped
    command: '--config=/etc/otel/config.yaml'
    volumes:
      - ./otel-collector/config.yaml:/etc/otel/config.yaml:ro
    environment:
      - CLICKHOUSE_PASSWORD=default
    depends_on:
      clickhouse:
        condition: service_healthy
      prometheus:
        condition: service_healthy
    network_mode: host
  rivet-engine:
    build:
      context: ../../..
      dockerfile: engine/docker/universal/Dockerfile
      target: engine-full
      args:
        BUILD_FRONTEND: 'true'
    platform: linux/amd64
    restart: unless-stopped
    environment:
      - RUST_LOG_ANSI_COLOR=1
      - RIVET_OTEL_ENABLED=1
      - RIVET_OTEL_SAMPLER_RATIO=1
      - RIVET_OTEL_GRPC_ENDPOINT=http://127.0.0.1:4317
    stop_grace_period: 0s
    depends_on:
      vector-client:
        condition: service_started
      otel-collector:
        condition: service_started
      postgres:
        condition: service_healthy
    volumes:
      - ./rivet-engine/config.jsonc:/etc/rivet/config.jsonc:ro
    healthcheck:
      test:
        - CMD
        - curl
        - '-f'
        - http://127.0.0.1:6420/health
      interval: 2s
      timeout: 10s
      retries: 10
      start_period: 30s
    network_mode: host
  runner:
    build:
      context: ../../..
      dockerfile: engine/sdks/typescript/test-runner/Dockerfile
    platform: linux/amd64
    restart: unless-stopped
    environment:
      - RIVET_ENDPOINT=http://127.0.0.1:6420
      - RIVET_RUNNER_TOTAL_SLOTS=1000000
    stop_grace_period: 4s
    depends_on:
      rivet-engine:
        condition: service_healthy
    network_mode: host
networks:
  rivet-core-network:
    driver: bridge
  rivet-network:
    driver: bridge
  rivet-network-engine-peer:
    driver: bridge
  rivet-network-to-core:
    driver: bridge
volumes:
  clickhouse-data: null
  prometheus-data: null
  grafana-data: null
  postgres-data: null
  vector-server-data-default: null
  vector-client-data-default: null
