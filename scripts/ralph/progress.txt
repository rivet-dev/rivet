## Codebase Patterns
- BARE schema files live in engine/sdks/schemas/{protocol-name}/v{N}.bare with no build step. They are plain text schemas.
- Use MARK comments (# MARK: Section) to organize sections in BARE schemas, matching runner-protocol conventions.
- The runner-protocol directory only has .bare files (no .ts or codegen in the same dir). TypeScript versioned handlers are separate.
- KV types in kv-channel-protocol mirror runner-protocol KV types but omit KvListRequest, KvDropRequest, and KvMetadata.
- TypeScript protocol packages live in engine/sdks/typescript/{protocol-name}/ with package.json, tsconfig.json, tsup.config.ts, and src/index.ts.
- The runner-protocol TS is auto-generated from BARE via build.rs; kv-channel-protocol is hand-written following the same read/write/encode/decode pattern.
- BARE void types in unions are represented as `null` values (no read/write call needed, just the tag byte).
- New pnpm workspace packages must be added to pnpm-workspace.yaml.
- The driver already has `kvDeleteRange(actorId, start, end)` on the actor driver interface. Wire it through `DatabaseProviderContext.kv` when adding KV range operations.
- `deleteRange` uses half-open `[start, end)` semantics everywhere (driver, file-system global-state, KV instance).
- For chunk key range bounds, use `getChunkKeyRangeEnd(fileTag)` which returns a 4-byte key `[PREFIX, VERSION, CHUNK_PREFIX, fileTag+1]` that is lexicographically past all 8-byte chunk keys for that fileTag.
- The sqlite-native crate is NOT in the root Cargo workspace. It has its own `[workspace]` table in Cargo.toml to prevent auto-discovery. Run `cargo check` from within `rivetkit-typescript/packages/sqlite-native/`.
- napi-rs v2 is used for the native addon. `crate-type = ["cdylib"]`, `napi-build` in build-dependencies, `napi`+`napi-derive` in dependencies.
- Platform npm packages under `npm/` follow naming: `@rivetkit/sqlite-native-{platform}` with `os`/`cpu` constraints.
- serde_bare `#[derive(Serialize, Deserialize)]` on enums produces byte-compatible BARE unions. Variant order must match schema tag order. Unit variants = void types, newtype variants = struct types.
- `tokio::spawn` requires `Send + 'static` on captured types. When using generic stream/sink types with `tokio::spawn`, add those bounds explicitly.
- `futures-util` is needed for `SinkExt`/`StreamExt` traits when working with `tokio-tungstenite` WebSocket streams (split, next, send).
- Reconnect backoff constants: initial=1000ms, max=30000ms, multiplier=2, jitter=0-25%. Matches `engine/sdks/typescript/runner/src/utils.ts`.
- libsqlite3-sys (bundled) exports `sqlite3_close` not `sqlite3_close_v2`. Use `sqlite3_close` for closing databases.
- Use `std::mem::zeroed()` to initialize `sqlite3_vfs` and `sqlite3_io_methods` structs, then set individual fields. This avoids needing to know the exact types of optional v2+/v3+ fields.
- File metadata encoding: 2 bytes u16 LE version + 8 bytes u64 LE size = 10 bytes total. Must match vbare's `serializeWithEmbeddedVersion` format (u16 LE prefix, not u8).
- VFS callbacks are sync C functions. Bridge to async KV channel via `rt_handle.block_on()`. Must not be called from within a tokio async context.
- Do NOT glob import `libsqlite3_sys::*` in files that also use `napi::bindgen_prelude::*`. Both export `Error` and `Result`, causing ambiguity. Use specific imports from libsqlite3-sys instead.
- `napi` crate needs `serde-json` feature to convert `serde_json::Value` to/from JS values. Add `serde_json = "1"` as a separate dependency.
- For napi-rs global tokio runtime: use `std::sync::OnceLock<Runtime>` with `get_or_init`. Use `rt.enter()` for `tokio::spawn`, `rt.block_on()` for sync waits.
- `sqlite3_column_type` returns `c_int` values. Define `SQLITE_TYPE_TEXT = 3` locally to avoid relying on inconsistent libsqlite3-sys export names (`SQLITE3_TEXT` vs `SQLITE_TEXT`).
- Use `std::mem::transmute(-1isize)` to create the SQLITE_TRANSIENT destructor function pointer for `sqlite3_bind_text`/`sqlite3_bind_blob`.
- ManagerDriver interface has 3 real implementations (FileSystem, Remote, Cloudflare) plus 2 stubs (test-inline-client-driver, manager-openapi-gen). All must be updated when changing the interface.
- The actor gateway middleware in `gateway.ts` intercepts WebSocket upgrades. New non-actor WebSocket endpoints need an explicit skip (`c.req.path.endsWith(...)`).
- `conn.disconnect()` in `handleAction`'s finally block is fire-and-forget (not awaited). The `pendingDisconnectCount` prevents the sleep timer from starting, but does NOT prevent `onStop` from running if sleep was already scheduled via `setImmediate`. Always wait for `pendingDisconnectCount === 0` before `#cleanupDatabase()` in the stop flow.
- Engine crates: directory `engine/packages/{name}/`, add to both `[workspace.members]` and `[workspace.dependencies.{name}]` in root Cargo.toml.
- For shared state across WebSocket connections in the guard, create the handler once in `create_routing_function` and clone the Arc per routing call.
- Native addon detection: use try-catch around `require('@rivetkit/sqlite-native')`, cache the result. The `getRequireFn()` from `@/utils/node` provides a require function that works in both CJS and ESM.
- `sqlite3_prepare_v2` with non-null tail pointer iterates through multi-statement SQL. Loop until stmt is null or tail reaches end.
- KV channel WebSocket URL: replace `http://`→`ws://`, `https://`→`wss://` in RIVET_ENDPOINT and append `/kv/connect`.
- VFS integration tests must use `#[test]` not `#[tokio::test]` because VFS callbacks use `Handle::block_on()` which panics inside async contexts.
- SQLite VACUUM fails with the KV VFS (temp file paths don't match file tag resolution). Use `PRAGMA journal_mode=truncate` to test xTruncate instead.
- Rust 2021 captures struct fields individually in closures. A `Wrapper(*mut T)` with `unsafe impl Send` doesn't work if the closure accesses `.0`. Use `ptr as usize` / `usize as *mut T` instead.
- napi async fn requires `JsNativeDatabase: Sync` because the future captures `&JsNativeDatabase` which must be `Send`.
- `watch::Sender::subscribe()` creates a receiver starting at the current value. Use watch channels (not Notify) when the signal may arrive before the waiter registers.
- For test gating, use `Semaphore::new(0)` + `add_permits(N)`. Avoids Notify race conditions and works regardless of waiter/signaler ordering.
- To clone an owned `T` from a `&T` reference, use UFCS `Clone::clone(ref)`. The method syntax `ref.clone()` returns `&T` due to `<&T as Clone>` taking priority in method resolution.
- For spawning tasks from functions that take reference parameters, use `Clone::clone(param)` to produce owned values for the spawned future.

# Ralph Progress Log
Started: Tue Mar 17 05:27:07 PM PDT 2026
---

## 2026-03-17 17:28 - US-001
- Created engine/sdks/schemas/kv-channel-protocol/v1.bare with all 23 required types
- Schema matches the spec doc (docs-internal/engine/NATIVE_SQLITE_DATA_CHANNEL.md lines 86-233) exactly
- Files changed: engine/sdks/schemas/kv-channel-protocol/v1.bare (new)
- **Learnings for future iterations:**
  - BARE schemas in this repo are plain text files with no build/typecheck step
  - runner-protocol uses hard tabs for indentation inside struct/union bodies
  - The kv-channel-protocol is a simpler subset of runner-protocol KV types (no list, drop, metadata)
  - Schema sections are organized with # MARK: comments
---

## 2026-03-17 - US-002
- Created @rivetkit/engine-kv-channel-protocol TypeScript package at engine/sdks/typescript/kv-channel-protocol/
- Hand-wrote all BARE types and read/write/encode/decode functions matching v1.bare schema exactly
- Exports: PROTOCOL_VERSION, all types (Id, KvKey, KvValue, RequestData, ResponseData, ToServer, ToClient, etc.), encodeToServer, decodeToServer, encodeToClient, decodeToClient, plus read/write functions for ToServer and ToClient
- Added package to pnpm-workspace.yaml
- Files changed: engine/sdks/typescript/kv-channel-protocol/{package.json, tsconfig.json, tsup.config.ts, src/index.ts} (new), pnpm-workspace.yaml (modified)
- **Learnings for future iterations:**
  - Runner-protocol TS is auto-generated via build.rs calling bare-ts compiler, but hand-writing is fine for simpler protocols
  - The bare-ts API: readU8/writeU8 for union tags, readU32/writeU32 for u32, readI64/writeI64 for i64, readString/writeString for str, readData/writeData for data, readUintSafe/writeUintSafe for list lengths
  - Package version should match other @rivetkit packages (2.1.6 currently)
  - tsup.config.ts extends ../../../../tsup.base and tsconfig.json extends ../../../../tsconfig.base.json
---

## 2026-03-17 17:40 - US-003
- Added `deleteRange(start, end)` to `KvVfsOptions` interface in `types.ts`
- Added `getChunkKeyRangeEnd(fileTag)` helper to `kv.ts` for constructing exclusive end bounds
- Migrated `xTruncate` from enumerating chunks + `deleteBatch` to O(1) `deleteRange`
- Migrated `#delete` from enumerating chunks + `deleteBatch` to O(1) `deleteRange` + single `deleteBatch([metaKey])`
- Added `deleteRange` to `DatabaseProviderContext.kv` in `config.ts` and wired through `mod.ts`
- Updated `createActorKvStore` in `shared.ts` to delegate to `kv.deleteRange`
- Updated test `createKvStore` in `sqlite-vfs.test.ts` with in-memory range delete implementation
- All 8 sqlite-vfs tests pass
- Files changed: sqlite-vfs/src/{types.ts, kv.ts, vfs.ts}, rivetkit/src/db/{config.ts, shared.ts}, rivetkit/src/actor/instance/mod.ts, sqlite-vfs-test/tests/sqlite-vfs.test.ts
- **Learnings for future iterations:**
  - The actor driver already exposes `kvDeleteRange` - no need to add it to the driver interface
  - `DatabaseProviderContext.kv` is the bridge between the driver and the VFS. New KV operations must be added here and in `mod.ts` where the context is constructed
  - Pre-existing typecheck errors in rivetkit (vitest imports, sandbox module, fixture types) are not blockers
  - The `getChunkKeyRangeEnd` helper uses a shorter key (4 bytes vs 8) that is still lexicographically greater than any chunk key for the same fileTag due to byte comparison semantics
---

## 2026-03-17 - US-004
- Added PRAGMA page_size = 4096 and PRAGMA busy_timeout = 5000 after sqlite3.open_v2() in SqliteVfs.open()
- Updated comment to reference native VFS and spec doc, removed stale TODO about PRAGMA benchmarking
- PRAGMAs run inside sqliteMutex to stay consistent with other sqlite3 calls
- Files changed: rivetkit-typescript/packages/sqlite-vfs/src/vfs.ts
- **Learnings for future iterations:**
  - sqlite3.exec(handle, sql) is the way to run PRAGMA statements on the raw WASM SQLite handle
  - All sqlite3 operations must be wrapped in the sqliteMutex to avoid concurrent access issues
  - page_size=4096 matches CHUNK_SIZE, busy_timeout=5000 matches the file-system driver default
  - WAL mode is intentionally not enabled for KV-backed SQLite
---

## 2026-03-17 - US-005
- Created rivetkit-typescript/packages/sqlite-native/ package scaffold
- Cargo.toml with napi-rs v2, libsqlite3-sys (bundled), tokio, tokio-tungstenite, serde, serde_bare
- package.json for @rivetkit/sqlite-native with napi-rs config for 5 platform targets
- build.rs with napi_build::setup()
- src/lib.rs with mod declarations for kv, protocol, channel, vfs
- src/kv.rs, src/protocol.rs, src/channel.rs, src/vfs.rs as empty modules with doc comments
- npm/ directory with 5 platform packages: linux-x64-gnu, linux-arm64-gnu, darwin-x64, darwin-arm64, win32-x64-msvc
- Crate excluded from root Cargo workspace via empty [workspace] table
- cargo check passes cleanly from within rivetkit-typescript/packages/sqlite-native/
- Files changed: rivetkit-typescript/packages/sqlite-native/{Cargo.toml, Cargo.lock, build.rs, package.json, src/lib.rs, src/kv.rs, src/protocol.rs, src/channel.rs, src/vfs.rs, npm/*/package.json} (all new)
- **Learnings for future iterations:**
  - Rust crates outside the Cargo workspace need `[workspace]` in their own Cargo.toml to prevent cargo from trying to find the root workspace
  - napi-rs v2 uses `napi-build = "2"` in build-dependencies with `extern crate napi_build; fn main() { napi_build::setup(); }` in build.rs
  - The `#[macro_use] extern crate napi_derive;` pattern is not needed if no `#[napi]` attributes are used yet in stubs
  - libsqlite3-sys v0.30 with `bundled` feature compiles and links SQLite statically
  - serde_bare v0.5 provides BARE serialization for Rust structs
  - edition 2021 is used for compatibility with napi-rs v2
---

## 2026-03-17 - US-006
- Implemented kv.rs with all constants, key functions, and 14 unit tests
- Constants: CHUNK_SIZE, SQLITE_PREFIX, SQLITE_SCHEMA_VERSION, META_PREFIX, CHUNK_PREFIX, FILE_TAG_MAIN/JOURNAL/WAL/SHM
- Functions: get_meta_key, get_chunk_key, get_chunk_key_range_end
- All tests verify byte-identical output to TypeScript kv.ts implementation
- Files changed: rivetkit-typescript/packages/sqlite-native/src/kv.rs
- **Learnings for future iterations:**
  - The Rust kv.rs uses fixed-size arrays ([u8; 4] and [u8; 8]) instead of Vec, which is more efficient and matches the known key sizes
  - get_chunk_key_range_end returns [u8; 4] not [u8; 8], and lexicographic comparison of [8,1,1,1] > [8,1,1,0,FF,FF,FF,FF] works because byte 3 differs (1 > 0)
  - u32::to_be_bytes() gives the same encoding as TypeScript's manual bit shifting (>>> 24, >>> 16, etc.)
---

## 2026-03-17 - US-007
- Implemented protocol.rs with all BARE types from v1.bare schema using serde_bare derive macros
- Defined: Id, KvKey, KvValue, KvGetRequest, KvPutRequest, KvDeleteRequest, KvDeleteRangeRequest, ErrorResponse, KvGetResponse, RequestData (enum), ResponseData (enum), ToServerRequest, ToServerPong, ToServer (enum), ToClientResponse, ToClientPing, ToClient (enum)
- Added encode/decode functions: encode_to_server, decode_to_server, encode_to_client, decode_to_client
- 28 tests: 18 round-trip tests for every message variant, 7 cross-language byte compatibility tests verifying exact output matches TypeScript, 3 decode error tests
- Files changed: rivetkit-typescript/packages/sqlite-native/src/protocol.rs
- **Learnings for future iterations:**
  - serde_bare with `#[derive(Serialize, Deserialize)]` produces byte-identical output to the hand-written TypeScript BARE encoder for this protocol
  - BARE union tags are encoded as varints by serde_bare, which for values 0-127 are single bytes identical to the TypeScript u8 tags
  - Enum variant ORDER must match the BARE schema union tag order exactly. serde_bare uses positional variant indices, not names
  - The runner-protocol generated code (vbare_compiler) uses the same pattern: unit variants for void types, newtype variants for struct types
  - Void BARE types in unions become unit enum variants with no data (just the tag byte on wire)
  - Vec<u8> in serde_bare encodes identically to BARE `data` (varint length + raw bytes)
---

## 2026-03-17 - US-008
- Implemented channel.rs with full WebSocket KV channel client
- KvChannel struct with connect(), send_request(), open_actor(), close_actor(), disconnect()
- Request correlation via requestId -> oneshot channel map
- requestId counter (u32, wrapping, resets to 0 on reconnect)
- Exponential backoff reconnect: initial=1000ms, max=30000ms, multiplier=2, jitter=0-25%
- On reconnect, re-sends ActorOpenRequest for all open actors (optimistic, no wait)
- On disconnect, fails all in-flight requests with ConnectionClosed error
- Handles ToClientPing by responding with ToServerPong (echoes timestamp)
- Handles ToClientClose by breaking read loop to trigger reconnect
- 30-second timeout per KV operation (KV_EXPIRE = 30_000ms)
- Background connection loop via tokio::spawn with watch channel for shutdown
- Read/write split: read loop in main task, write forwarding in spawned task
- Added futures-util dependency to Cargo.toml for SinkExt/StreamExt traits
- 12 unit tests covering URL building, backoff calculation, error display, and constants
- Files changed: rivetkit-typescript/packages/sqlite-native/src/channel.rs, Cargo.toml, Cargo.lock
- **Learnings for future iterations:**
  - Generic stream/sink types moved into `tokio::spawn` need `Send + 'static` bounds
  - `futures-util` is required for `SinkExt`/`StreamExt` on tokio-tungstenite streams
  - `watch::Sender::subscribe()` creates a new receiver that can be moved into spawned tasks (avoids lifetime issues with `&mut` borrows)
  - For pseudo-random jitter without adding `rand` dependency, nanosecond component of system time works well enough for backoff purposes
  - Optimistic re-open after reconnect: send ActorOpenRequest messages before storing outgoing_tx, so they get buffered in the mpsc channel and forwarded first by the write task
  - `Message::Binary(bytes.into())` works regardless of whether tungstenite expects `Vec<u8>` or `Bytes`
---

## 2026-03-17 - US-009
- Implemented complete custom SQLite VFS in vfs.rs (~1000 lines) backed by KV operations via the channel
- VFS callbacks: xOpen, xClose, xRead, xWrite, xTruncate, xDelete, xSync, xFileSize, xAccess, xLock/xUnlock (no-op), xFullPathname, xRandomness, xSleep, xCurrentTime, xGetLastError, xCheckReservedLock, xFileControl, xSectorSize, xDeviceCharacteristics
- xRead: batch-fetch chunk keys via KvGetRequest, HashMap-based lookup for robust key matching, handles partial chunks and short reads
- xWrite: prefetch partial chunks if needed, batch-write via KvPutRequest, includes metadata in write batch when dirty
- xTruncate: KvDeleteRangeRequest for chunks beyond truncation point, optional partial chunk rewrite, metadata update
- xDelete: KvDeleteRangeRequest for all chunks + KvDeleteRequest for metadata key
- xSync: flush dirty metadata via KvPutRequest
- xOpen: resolve file path to file tag (main/journal/wal/shm), load existing metadata or create new file
- xClose: flush dirty metadata, handle SQLITE_OPEN_DELETEONCLOSE
- Metadata encoding: [version_u8, size_u64_le] = 9 bytes, byte-identical to WASM VFS vbare format
- VfsContext struct with sync KV helper methods (kv_get, kv_put, kv_delete, kv_delete_range)
- KvVfs public struct with register/drop lifecycle, auto-unregisters on drop
- NativeDatabase struct that owns both the database handle and VFS registration
- open_database helper that applies PRAGMA page_size=4096 and busy_timeout=5000
- 7 unit tests for metadata encoding/decoding and struct sizing
- All 62 tests pass, cargo check clean
- Files changed: rivetkit-typescript/packages/sqlite-native/src/vfs.rs
- **Learnings for future iterations:**
  - libsqlite3-sys (bundled) exports `sqlite3_close` not `sqlite3_close_v2`
  - Use `std::mem::zeroed()` to initialize `sqlite3_vfs` and `sqlite3_io_methods`, then set fields individually to avoid type issues with optional v2+/v3+ fields like xShmMap
  - VFS context stored in `pAppData` as a leaked Box pointer. IO methods stored in the context as a Box. Both outlive all files.
  - File path to tag resolution: exact match for main, suffix match for -journal/-wal/-shm using strip_prefix
  - KvGetResponse may only return found keys. Use HashMap-based lookup (build_value_map) for robust matching instead of positional indexing
  - The sqlite3_file struct must be extended via `#[repr(C)]` with `base: sqlite3_file` as the first field and `szOsFile` set to the full struct size
---

## 2026-03-17 - US-010
- Implemented napi-rs entry point in lib.rs with all 6 exported functions
- connect(config) -> JsKvChannel: creates KV channel WebSocket connection using global tokio runtime
- openDatabase(channel, actorId) -> JsNativeDatabase: sends ActorOpenRequest, registers VFS, opens SQLite database
- execute(db, sql, params?) -> ExecuteResult: prepares statement, binds params, steps, returns changes count
- query(db, sql, params?) -> QueryResult: prepares statement, binds params, reads columns and rows
- closeDatabase(db): drops database/VFS, sends ActorCloseRequest to release server lock
- disconnect(channel): shuts down the WebSocket connection
- Global tokio runtime via OnceLock<Runtime>, initialized once per process
- Parameter binding supports null, bool, number (i64/f64), string, and blob (number array)
- Column extraction supports NULL, INTEGER, FLOAT, TEXT, and BLOB types via serde_json::Value
- Added serde_json dependency and napi serde-json feature for JS <-> Rust value conversion
- All 62 existing tests pass, cargo check clean
- Files changed: rivetkit-typescript/packages/sqlite-native/src/lib.rs, Cargo.toml, Cargo.lock
- **Learnings for future iterations:**
  - `libsqlite3_sys::*` and `napi::bindgen_prelude::*` both export `Error` and `Result`. Never glob import both. Use specific imports from libsqlite3-sys.
  - napi's `serde-json` feature enables automatic `FromNapiValue`/`ToNapiValue` for `serde_json::Value`, which is ideal for dynamic SQL parameter/result types
  - `#[napi(js_name = "KvChannel")]` on a struct creates a JS class with the specified name. Functions returning it use `JsKvChannel` in Rust but `KvChannel` in JS.
  - `rt.enter()` is only needed when calling `tokio::spawn` outside async context (e.g., in `connect`). `rt.block_on()` provides its own context.
  - `sqlite3_prepare_v2` with `nByte = -1` reads SQL up to NUL terminator, simplifying CString usage
  - SQLITE_TRANSIENT = `transmute(-1isize)` as a function pointer, tells SQLite to copy bound data immediately
  - The TEXT column type constant (3) varies in name between libsqlite3-sys versions (SQLITE3_TEXT vs SQLITE_TEXT). Safest to define locally.
---

## 2026-03-17 - US-011
- Added KV channel WebSocket endpoint at GET /kv/connect in manager router
- Created kv-channel.ts handler with full BARE protocol deserialization/serialization
- Authentication: validates token against config.token (RIVET_TOKEN), skips with warning in dev mode if not set
- Protocol version validation (protocol_version=1)
- Single-writer lock per actor (Map-based, per connection)
- Full KV operation handling: ActorOpen/Close, KvGet, KvPut, KvDelete, KvDeleteRange
- All error codes from spec: actor_locked, actor_not_open, key_too_large, value_too_large, payload_too_large, batch_too_large, storage_quota_exceeded, keys_values_length_mismatch, unauthorized, internal_error
- KV limits enforcement matching kv-limits.ts constants (2048 byte keys, 128 KiB values, 128 batch entries, 976 KiB payload)
- Ping every 3 seconds, close connection if no pong within 15 seconds
- On disconnect, releases all actor locks held by the connection
- Extended ManagerDriver interface with kvBatchGet, kvBatchPut, kvBatchDelete, kvDeleteRange
- Implemented batch KV methods in FileSystemManagerDriver, stub implementations in RemoteManagerDriver and CloudflareActorsManagerDriver
- Updated gateway.ts to skip /kv/connect path (prevents actor gateway from intercepting the WebSocket upgrade)
- Added @rivetkit/engine-kv-channel-protocol dependency to rivetkit package
- Updated manager-openapi-gen.ts and test-inline-client-driver.ts stubs for new ManagerDriver methods
- Files changed: rivetkit/src/manager/{kv-channel.ts (new), router.ts, gateway.ts, driver.ts}, rivetkit/package.json, pnpm-lock.yaml, rivetkit/scripts/manager-openapi-gen.ts, rivetkit/src/driver-test-suite/test-inline-client-driver.ts, rivetkit/src/remote-manager-driver/mod.ts, rivetkit/src/drivers/file-system/manager.ts, cloudflare-workers/src/manager-driver.ts
- **Learnings for future iterations:**
  - The ManagerDriver interface is implemented in 3 places: FileSystemManagerDriver, RemoteManagerDriver, CloudflareActorsManagerDriver. All must be updated when adding methods.
  - There's also a test mock in test-inline-client-driver.ts and a stub in manager-openapi-gen.ts that must satisfy the interface.
  - The Hono `WSContext.send()` expects `string | ArrayBuffer | Uint8Array<ArrayBuffer>`. Passing `Uint8Array` from `encodeToClient` may fail due to `ArrayBufferLike` vs `ArrayBuffer` mismatch. Copy to a fresh `ArrayBuffer` to be safe.
  - The actor gateway middleware (`gateway.ts`) intercepts all WebSocket upgrades and tries to route them to actors. New WebSocket endpoints must be skipped in the gateway (see `c.req.path.endsWith("/kv/connect")`).
  - KvGetResponse returns only found keys and values (not null entries). The client must match by key, not by positional index.
  - `Extract<UnionType, { readonly tag: "Variant" }>` is useful for narrowing discriminated union types in helper functions.
---

## 2026-03-17 - US-012
- Created engine/packages/pegboard-kv-channel/ crate with full WebSocket handler for the KV channel protocol
- protocol.rs: BARE serialization types matching v1.bare schema exactly using serde_bare derive macros, with 5 round-trip tests
- lib.rs: CustomServeTrait implementation with full WebSocket lifecycle management
  - Single-writer lock per actor via shared HashMap<String, u64> (actor_id -> conn_id)
  - ActorOpenRequest/ActorCloseRequest for lock acquire/release
  - KvGet/KvPut/KvDelete/KvDeleteRange routed to pegboard::actor_kv module
  - All error codes from spec: actor_locked, actor_not_open, actor_not_found, key_too_large, value_too_large, payload_too_large, batch_too_large, storage_quota_exceeded, keys_values_length_mismatch, internal_error
  - KV limits enforced (2048 byte keys, 128 KiB values, 128 batch entries, 976 KiB payload)
  - Ping every 3s (runner_update_ping_interval_ms), close if no pong within 15s (runner_ping_timeout_ms)
  - On disconnect, releases all locks held by the connection
  - On SIGTERM, sends ToClientClose before closing WebSocket
  - Namespace resolution via namespace::ops::resolve_for_name_global
  - Actor lookup via pegboard::ops::actor::get_for_runner for name/existence validation
- Added pegboard-kv-channel to root Cargo.toml workspace members and dependencies
- Created guard routing module (kv_channel.rs) for path-based routing at /kv/connect
  - Auth: validates token query param against auth.admin_token
  - Regional host validation matching runner routing pattern
  - Shared handler instance across all connections for lock table sharing
- Updated guard Cargo.toml and routing/mod.rs to wire in KV channel routing
- Files changed: Cargo.toml, Cargo.lock, engine/packages/pegboard-kv-channel/{Cargo.toml, src/lib.rs, src/protocol.rs} (new), engine/packages/guard/{Cargo.toml, src/routing/mod.rs, src/routing/kv_channel.rs}
- **Learnings for future iterations:**
  - Engine crates follow naming convention: directory `engine/packages/{name}/`, Cargo package name matches directory name
  - New workspace members need both `[workspace.members]` and `[workspace.dependencies.{name}]` entries in root Cargo.toml
  - The guard routing function creates handlers per-request by default (like pegboard-runner), but for shared state (like lock tables), create the handler once in `create_routing_function` and clone the Arc
  - `auth.admin_token.read()` returns a borrow; use `*auth.admin_token.read()` when comparing against an owned String
  - Pre-existing build errors exist in the codebase (proxy.rs, websocket.rs, etc.) but don't affect compilation of targeted crate checks
  - The `gas::prelude::*` import provides `Id`, `Result`, `StandaloneCtx`, `Context`, tracing macros, etc.
  - `CustomServeTrait` requires `Send + Sync` (via Arc<dyn CustomServeTrait>), which is satisfied by Mutex<HashMap> + AtomicU64
  - KV channel protocol uses the same `Vec<u8>` types as runner protocol for KvKey/KvValue, so data passes directly to actor_kv without conversion
---

## 2026-03-17 - US-013
- Added `nativeSqliteAvailable()` detection function in `db/native-sqlite.ts` that try-catches require('@rivetkit/sqlite-native')
- Added process-level singleton KV channel connection via `getOrCreateKvChannel()`, deriving WebSocket URL from RIVET_ENDPOINT env var
- Added `createNativeRawAccess(actorId)` that wraps the native addon's query/execute/exec into the `RawAccess` interface
- Updated `db/mod.ts` with conditional path: native SQLite first, then WASM fallback with console.warn
- Added `exec` function to native addon's lib.rs for multi-statement SQL support (loops sqlite3_prepare_v2 with tail pointer)
- Added `toNativeBindings` helper to convert bigint and Uint8Array to JSON-compatible types for the native addon
- Warning message logged at most once per process via `nativeFallbackWarned` flag
- Database interface (RawAccess.execute/close) is identical regardless of backend
- Files changed: rivetkit/src/db/{native-sqlite.ts (new), mod.ts}, sqlite-native/src/lib.rs
- **Learnings for future iterations:**
  - The native addon's napi functions accept `serde_json::Value` via the `serde-json` napi feature. BigInt and Uint8Array must be converted to number/number[] before passing.
  - `toSqliteBindings` returns a `SqliteBindings` type from the WASM VFS, not `unknown[]`. For the native path, validate/convert independently.
  - `sqlite3_prepare_v2` with a tail pointer (`*const c_char`) enables multi-statement SQL iteration. The tail points to the start of the next unprepared statement.
  - The KV channel WebSocket URL is derived from RIVET_ENDPOINT by replacing http(s) with ws(s) and appending `/kv/connect`.
  - Pre-existing typecheck errors in fixtures/driver-test-suite, engine driver, and sandbox module are not related to db changes.
  - The fallback warning uses `console.warn` rather than pino logger since the db module doesn't have access to a logger instance at the module level.
---

## 2026-03-17 - US-014
- Updated `scripts/release/update_version.ts` with 3 new entries:
  - `rivetkit-typescript/packages/sqlite-native/npm/*/package.json` for platform package versions
  - `rivetkit-typescript/packages/sqlite-native/package.json` optionalDependencies versions (regex matches `@rivetkit/sqlite-native-*` dep entries)
  - `rivetkit-typescript/packages/sqlite-native/Cargo.toml` version field (not in root workspace, needs explicit bump)
- Updated `scripts/release/sdk.ts`:
  - Added `BUILD_EXCLUDED_RIVETKIT_PACKAGES` array for packages excluded from turbo build but still published
  - `@rivetkit/sqlite-native` excluded from build step (requires Rust/napi-rs, not buildable by turbo)
  - Added explicit publish loop for platform packages under `npm/` (not in pnpm workspace, need `npm publish` from their directory)
- Files changed: scripts/release/update_version.ts, scripts/release/sdk.ts
- **Learnings for future iterations:**
  - The main `@rivetkit/sqlite-native` package.json is already covered by the existing `rivetkit-typescript/packages/*/package.json` glob in update_version.ts. Only the nested npm/ packages and Cargo.toml need explicit entries.
  - `EXCLUDED_RIVETKIT_PACKAGES` excludes from both build AND publish. For native addons that need separate builds but still need publishing, use a separate exclusion list for the build step only.
  - Platform npm packages under `npm/` are NOT in the pnpm workspace. They must be published via `npm publish` directly from their directory, not via `pnpm --filter`.
  - The optionalDependencies in the main package.json pin exact versions of platform packages. These must be bumped alongside the platform package versions.
---

## 2026-03-17 - US-015
- Implemented integration test module with mock WebSocket KV server (12 tests, 920 lines)
- Mock server: in-process WebSocket server with BTreeMap-backed KV store per actor, single-writer locks, BARE protocol handling, operation logging, and broadcast-based connection killing for reconnection tests
- Tests cover all acceptance criteria:
  - test_basic_sql_through_vfs: xRead/xWrite through SQLite CREATE TABLE/INSERT/SELECT, verifies chunk 0 and metadata keys in KV store
  - test_multi_chunk_data: inserts enough data to span 2+ chunks, verifies sequential chunk indices and file size
  - test_chunk_boundary_data_integrity: 50 rows with 500-char payloads, verifies data reads back identically across chunk boundaries
  - test_truncate_produces_delete_range: PRAGMA journal_mode=truncate, verifies DeleteRange ops with correct SQLITE_PREFIX/CHUNK_PREFIX format
  - test_delete_journal_produces_correct_ranges: default journal_mode=delete, verifies DeleteRange bounds match chunk_key(JOURNAL, 0)..chunk_key_range_end(JOURNAL)
  - test_metadata_tracks_file_size: verifies decoded metadata shows page-aligned file size >= 4096
  - test_close_flushes_and_reopen_preserves_data: write, close, reopen actor, verify data persists (metadata flushed on xClose)
  - test_file_tags_encoding: verifies main and journal file tags appear in correct byte positions in KV keys
  - test_error_actor_not_open: KV request without opening actor returns actor_not_open error
  - test_error_actor_locked: second channel opening same actor returns actor_locked error
  - test_optimistic_open_pipelining: 5 concurrent KV requests succeed with correct request correlation
  - test_reconnection_reopens_actors: force-close connection, wait for reconnect, verify data persists and actor reopened >= 2 times
- All 74 tests pass (62 existing + 12 new), cargo test completes in ~3.5s
- Files changed: rivetkit-typescript/packages/sqlite-native/src/{integration_tests.rs (new), lib.rs}
- **Learnings for future iterations:**
  - VFS callbacks use `Handle::block_on()` which panics inside async contexts. Integration tests must use `#[test]` (not `#[tokio::test]`) with manual `Runtime::new()` and call VFS operations from the test thread (outside `rt.block_on`).
  - Drop order matters: NativeDatabase must be dropped before Runtime, since xClose calls Handle::block_on. Rust's reverse-declaration-order drop ensures this when db is declared after rt.
  - `tokio::sync::broadcast` is useful for signaling connection kills in mock servers. Each connection handler subscribes and breaks from its read loop when the signal fires.
  - BTreeMap's `range(start..end)` implements the same half-open `[start, end)` semantics as the KV channel's deleteRange, making mock server implementation straightforward.
  - SQLite VACUUM fails with the KV VFS because it creates temporary files with paths that don't match the VFS's file tag resolution (main, -journal, -wal, -shm). Use PRAGMA journal_mode=truncate to test xTruncate instead.
  - `tokio_tungstenite::accept_async` accepts any WebSocket upgrade regardless of URL path, so mock servers don't need HTTP routing.
  - Each test uses a unique VFS name via AtomicU64 counter to avoid SQLite global VFS registry conflicts in parallel test runs.
---

## 2026-03-17 - US-016
- Changed `execute`, `query`, and `exec` napi functions from sync to async (`#[napi] pub async fn`)
- Wrapped all SQLite work (prepare, bind, step, column reads) inside `get_runtime().spawn_blocking(move || { ... }).await`
- Added `unsafe impl Sync for JsNativeDatabase` to satisfy napi async fn `Send` requirement on the future
- Used `usize` casting for `*mut sqlite3` pointer to cross thread boundaries (Rust 2021 captures individual struct fields, so a `SendableDbPtr` wrapper doesn't work)
- Added detailed code comment documenting 3 threading approaches (spawn_blocking, dedicated thread, channel+block-in-place) with reference to NATIVE_SQLITE_REVIEW_FINDINGS.md Finding 1
- Updated TypeScript `NativeSqliteModule` interface: `execute`, `query`, `exec` now return `Promise<...>`
- Added `await` to all native addon call sites in `native-sqlite.ts`
- All 74 Rust tests pass, TypeScript typecheck passes
- Files changed: rivetkit-typescript/packages/sqlite-native/src/lib.rs, rivetkit-typescript/packages/rivetkit/src/db/native-sqlite.ts
- **Learnings for future iterations:**
  - Rust 2021 edition closures capture individual struct fields, not the whole struct. A `SendableDbPtr(*mut T)` wrapper with `unsafe impl Send` does NOT work because `move || { db_ptr.0 }` captures the inner `*mut T` field directly, bypassing the Send impl. Use `ptr as usize` before the closure and `usize as *mut T` inside instead.
  - napi async fn requires the future to be `Send + 'static`. `&JsNativeDatabase` is `!Send` if the struct contains `*mut T` (which is `!Sync`), because `&T: Send` requires `T: Sync`. Fix with `unsafe impl Sync for JsNativeDatabase`.
  - `tokio::spawn_blocking` uses a separate thread pool (default cap 512) from the async worker pool, so VFS `Handle::block_on()` calls never starve the WebSocket read/write tasks.
  - When making napi functions async, the TypeScript callers must add `await`. In this codebase, the calls were already inside `async` arrow functions, so adding `await` was straightforward.
---

## 2026-03-17 - US-017
- Changed `actor_locks` type from `HashMap<String, u64>` to `HashMap<String, (u64, Arc<Mutex<HashSet<String>>>)>` to store a reference to each connection's open_actors set
- Updated `handle_actor_open` to unconditionally evict old locks when a different connection opens the same actor. On eviction, removes the actor from the old connection's open_actors set via the stored Arc reference.
- Updated `handle_actor_close` to use `if let Some((lock_conn, _))` pattern for the new tuple type
- Updated disconnect cleanup to only release locks still held by this conn_id (another connection may have evicted it)
- Threaded `&Arc<Mutex<HashSet<String>>>` through `message_loop`, `handle_binary_message`, `handle_request`, `handle_actor_open`, and `handle_actor_close` function signatures (previously `&Mutex<HashSet<String>>`)
- cargo check passes cleanly for pegboard-kv-channel
- Files changed: engine/packages/pegboard-kv-channel/src/lib.rs
- **Learnings for future iterations:**
  - The `open_actors` parameter was already `Arc<Mutex<HashSet<String>>>` at the `handle_websocket` level. Changing function signatures from `&Mutex<...>` to `&Arc<Mutex<...>>` required no call-site changes because Rust auto-deref works in reverse (passing `&arc_val` matches `&Arc<T>` directly).
  - Eviction under the `actor_locks` mutex is atomic. The old connection's fast-path check (`open_actors.contains`) fails immediately after eviction. No global lock check on the KV hot path.
  - Pre-existing build errors in guard crate (proxy.rs, websocket.rs, custom_serve.rs) are unrelated to pegboard-kv-channel changes. Use `cargo check -p pegboard-kv-channel` for targeted verification.
---

## 2026-03-17 - US-018
- Updated `handleActorOpen` in manager kv-channel.ts to unconditionally evict old connection locks on re-open
- When a different connection opens the same actor, the old connection's `openActors` set has the actor removed, causing its next KV request to fail with `actor_not_open`
- The lock is transferred to the new connection atomically (JS single-threaded, no race window)
- `cleanupConnection` already correctly checks `actorLocks.get(actorId) === conn` before deleting, so evicted locks are not double-freed
- No structural change to the lock map needed since `Map<string, KvChannelConnection>` already stores the connection reference (which includes `openActors`)
- Files changed: rivetkit-typescript/packages/rivetkit/src/manager/kv-channel.ts
- **Learnings for future iterations:**
  - The TypeScript manager is single-threaded (JS event loop), so lock eviction is inherently atomic. No mutex needed unlike the Rust engine implementation.
  - The existing `Map<string, KvChannelConnection>` already stores the full connection object including `openActors`, making it simpler than the engine's `HashMap<String, (u64, Arc<Mutex<HashSet<String>>>)>` approach.
  - Object identity comparison (`lockHolder !== conn`) in JS is equivalent to comparing connection IDs.
---

## 2026-03-17 - US-019
- Implemented client-side reconnect waiting: KV requests block until ActorOpenResponse is received on reconnect
- Added `reconnect_ready: HashMap<String, watch::Sender<bool>>` and `reconnect_request_ids: HashMap<u32, String>` to channel Inner struct
- On reconnect, watch channels are created per actor. `send_request` subscribes and waits for `true` before sending KV requests.
- Response handler routes reconnect ActorOpenResponses to mark actors ready. On error, removes actor from open_actors and drops sender (waiters get RecvError -> ConnectionClosed).
- Initial open (first connection) remains optimistic: no reconnect_ready entries, so send_request proceeds immediately.
- `fail_all_in_flight` also clears reconnect state on disconnect.
- Refactored mock server to concurrent message processing (spawned tasks per message, mpsc channel for response writing) to properly test client-side waiting.
- Added semaphore-based gate to mock server's ActorOpenRequest handler for testing.
- Added `test_reconnect_kv_waits_for_open_response` test that verifies KV requests are blocked while reconnect open is gated, then succeed after gate release.
- All 75 tests pass (62 unit + 13 integration), cargo check clean.
- Files changed: rivetkit-typescript/packages/sqlite-native/src/{channel.rs, integration_tests.rs}
- **Learnings for future iterations:**
  - `watch::Sender::subscribe()` creates a new receiver starting with the current value. If `true` was already sent, the new receiver sees it immediately. This avoids the Notify race where notification can be missed if the waiter registers after the signal.
  - Concurrent mock server (spawned tasks per message) is needed to properly test client-side waiting. Sequential processing masks the behavior because the server blocks on the open regardless.
  - `tokio::sync::Semaphore::new(0)` with `add_permits(N)` is a clean gate mechanism for tests. The semaphore blocks `acquire()` until permits are added, and the order of operations doesn't matter (unlike Notify).
  - The `!kv_handle.is_finished()` assertion distinguishes client-side waiting from server-side blocking only when the mock server processes messages concurrently.
---

## 2026-03-17 - US-020
- Implemented per-actor channel routing in engine KV channel server (pegboard-kv-channel)
- Added `HashMap<String, mpsc::UnboundedSender<ToServerRequest>>` for routing incoming requests to per-actor channels
- Each actor gets a spawned tokio task (`actor_request_task`) that drains its channel sequentially, preserving intra-actor ordering
- Cross-actor requests now execute concurrently (Actor A's slow KvPutRequest no longer blocks Actor B's KvGetRequest)
- Message loop wrapped in async block to ensure cleanup runs on all exit paths (? errors, normal close, term signal)
- On disconnect: drop all senders then join all tasks via JoinSet to drain in-flight requests
- On actor close: remove sender from HashMap so per-actor task exits after processing remaining requests
- Used `Clone::clone(ref)` UFCS syntax to clone owned values from reference parameters for spawned tasks (avoids `&T.clone()` returning `&T` instead of `T`)
- Changed `message_loop` and `handle_binary_message` state parameter from `&KvChannelState` to `&Arc<KvChannelState>` to enable cloning the Arc for spawned tasks
- cargo check passes cleanly for pegboard-kv-channel
- Files changed: engine/packages/pegboard-kv-channel/src/lib.rs
- **Learnings for future iterations:**
  - `ref.clone()` where `ref: &T` calls `<&T as Clone>::clone()` returning `&T`, not `T`. Use UFCS `Clone::clone(ref)` or `T::clone(ref)` to get an owned `T` from a reference.
  - Async blocks (`async { ... }.await`) are useful for ensuring cleanup runs after early returns. `return` inside an async block returns from the block, not the enclosing function.
  - `tokio::task::JoinSet` aborts all tasks on drop, so explicitly drain with `join_next()` if you want tasks to finish cleanly.
  - Deref coercion makes `&Arc<T>` auto-coerce to `&T` in function call arguments, so changing a parameter from `&T` to `&Arc<T>` doesn't require call-site changes.
  - Per-actor channels use unbounded mpsc to avoid backpressure issues. A single actor never has concurrent KV requests (VFS callbacks are sequential within SQLite), so the channel depth is effectively 1.
---

## 2026-03-17 - US-021
- Applied per-actor channel routing pattern from US-020 (pegboard-kv-channel) to the runner protocol's KV handling in ws_to_tunnel_task.rs
- Added `RunnerKvRequest` enum with `Mk1`/`Mk2` variants to hold both protocol versions
- Added `HashMap<String, mpsc::UnboundedSender<RunnerKvRequest>>` in `task_inner` for routing KV requests to per-actor channels
- Added `JoinSet` for managing per-actor tasks with proper cleanup on disconnect
- Modified `handle_message_mk2` and `handle_message_mk1` to route KV requests to per-actor channels instead of processing inline
- Extracted `process_kv_request_mk2` and `process_kv_request_mk1` standalone functions containing the original KV handling logic
- Added `actor_kv_task` function that drains per-actor channel sequentially and dispatches to the appropriate process function
- Wrapped `task_inner` loop in async block for cleanup on all exit paths (errors, lifecycle events)
- Removed `// TODO: Add queue and bg thread for processing kv ops` comments (now implemented)
- Cross-actor KV requests execute concurrently while preserving intra-actor ordering
- cargo check passes cleanly for pegboard-runner
- Files changed: engine/packages/pegboard-runner/src/ws_to_tunnel_task.rs
- **Learnings for future iterations:**
  - The runner protocol has both mk1 and mk2 message handling with different types and serialization. Per-actor routing needs an enum (`RunnerKvRequest`) to unify both variants under one channel.
  - `Clone::clone(ctx)` UFCS syntax is needed when cloning `&StandaloneCtx` for spawned tasks. Method syntax `ctx.clone()` would return `&StandaloneCtx` due to `<&T as Clone>` priority.
  - The `handle_message_mk1` function has a `compat_ack_tunnel_message` call before message parsing that applies to ALL messages, not just KV. This stays in the main handler, not the per-actor task.
  - `send_actor_kv_error` and `send_actor_kv_error_mk1` take `&Conn` which works with `&Arc<Conn>` via auto-deref. No changes needed to these helper functions.
  - The mk1 KV error response for actor_id parse failure was previously inline-constructed. Simplified to use `send_actor_kv_error_mk1` in the extracted function since it produces identical output.
---

## 2026-03-17 - US-022
- Updated docs-internal/engine/NATIVE_SQLITE_DATA_CHANNEL.md with all review findings from NATIVE_SQLITE_REVIEW_FINDINGS.md
- JavaScript Bindings section: changed `execute`, `query` signatures to return `Promise<...>`, added `exec` function, added threading note about `spawn_blocking` and `Handle::block_on()`
- Single-Writer Lock Semantics section: added "Unconditional lock eviction on reconnect" paragraph covering same-server eviction and multi-server deployment behavior
- Reconnect Behavior section: added note that client must wait for `ActorOpenResponse` before sending KV requests on reconnect (initial open remains optimistic)
- Files changed: docs-internal/engine/NATIVE_SQLITE_DATA_CHANNEL.md
- **Learnings for future iterations:**
  - The spec doc is a markdown file with no typecheck or build step. Changes are purely textual.
  - Review findings doc (NATIVE_SQLITE_REVIEW_FINDINGS.md) has a "Spec update needed" subsection in each finding that provides the exact wording to add.
  - Finding 2 (per-actor concurrency) explicitly says "No spec change needed" - it's implementation guidance only.
  - Finding 3 (thread pool exhaustion) is resolved by Finding 1, no separate spec change needed.
---

## 2026-03-18 - US-023
- Fixed race condition where `onDisconnect` DB writes fail with "Database not enabled" when actor sleep races with pending disconnect handlers
- Root cause: `startSleep()` uses `setImmediate(() => sleep())`. If an action arrives between `startSleep()` being called and the `setImmediate` callback firing, the action's `conn.disconnect()` starts an async `onDisconnect` handler. When the `setImmediate` fires, `sleepActor()` calls `onStop("sleep")` which runs `#cleanupDatabase()` (setting `#db = undefined`) without waiting for the pending disconnect handler. 250ms later, the handler tries `c.db.execute()` and gets "Database not enabled".
- Fix: Added `#waitForPendingDisconnects()` in the `onStop` flow after `#disconnectConnections()`. This polls `connectionManager.pendingDisconnectCount` until it reaches 0 (with a 5 second timeout), ensuring all fire-and-forget disconnect handlers complete before the database is cleaned up.
- Also added polling in the test assertion as a safety net for timing variance under load.
- Files changed:
  - `rivetkit-typescript/packages/rivetkit/src/actor/instance/mod.ts` - Added `#waitForPendingDisconnects()` method and call in `onStop`
  - `rivetkit-typescript/packages/rivetkit/src/driver-test-suite/tests/actor-db.ts` - Added polling loop in disconnect test
- **Learnings for future iterations:**
  - `handleAction`'s finally block calls `conn.disconnect()` WITHOUT awaiting it. The returned Promise is fire-and-forget. This creates a window where disconnect handlers run outside the stop flow's awareness.
  - `pendingDisconnectCount` prevents the sleep TIMER from starting, but does NOT prevent `onStop` from running if sleep was already deferred via `setImmediate`.
  - To diagnose flaky tests, grep test logs for "error in `onDisconnect`" to find swallowed errors in disconnect handlers.
  - The native SQLite KV channel is a process-level singleton connecting to `RIVET_ENDPOINT` (or `localhost:6420`). In tests, this connects to the dev server, not the test server.
---

## 2026-03-21 - US-024
- Fixed metadata encoding mismatch between WASM and native VFS
- Changed META_VERSION from u8 (1 byte) to u16 (2 bytes LE), matching vbare's `serializeWithEmbeddedVersion` format
- Updated META_ENCODED_SIZE from 9 to 10 (2 bytes version + 8 bytes size)
- Updated `encode_file_meta` to use `META_VERSION.to_le_bytes()` (2 bytes) instead of `push(META_VERSION)` (1 byte)
- Updated `decode_file_meta` to read 2-byte u16 LE version and extract size from bytes [2..10] instead of [1..9]
- Updated all 7 unit tests to reflect the new 10-byte layout
- Added `encode_matches_vbare_format` test to explicitly verify byte-level compatibility with vbare
- All 76 tests pass, cargo check clean
- Files changed: rivetkit-typescript/packages/sqlite-native/src/vfs.rs
- **Learnings for future iterations:**
  - vbare's `serializeWithEmbeddedVersion` prepends a 2-byte u16 LE version, not a varint or single byte. Verified by inspecting the published vbare@0.0.4 package source.
  - The WASM VFS (production format) produces 10-byte metadata: [version_u16_le, size_u64_le]. The native VFS must match exactly.
  - `DataView.setUint16(0, version, true)` in JS produces the same bytes as `version.to_le_bytes()` in Rust for u16 values.
---
