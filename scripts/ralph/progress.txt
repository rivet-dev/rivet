# Ralph Progress Log
Started: Fri Mar 20 12:05:03 AM PDT 2026
---

## Codebase Patterns
- SqliteRuntimeDatabase interface only has exec/run/get/all/close methods. No prepare() or columns(). Use all() which returns Record<string, unknown>[] then Object.keys() to get column names.
- KV databases use #actorKvDatabases map with lazy open via #getOrCreateActorKvDatabase. App databases follow the same pattern with #actorAppDatabases.
- Actor teardown happens in sleepActor (close db, delete runtime, remove entry) and destroyActor (close db, delete files, reset entry). Both paths must handle app databases.
- Bridge keys in DYNAMIC_HOST_BRIDGE_GLOBAL_KEYS are string constants prefixed with __rivetkitDynamicHost.
- Pre-existing typecheck errors exist in fixtures/workflow.ts, engine driver, and test files. These are not related to dynamic actor changes.
- There is no host-runtime.ts. Both isolated-vm and secure-exec use the same #setIsolateBridge() in isolate-runtime.ts.
- New ActorDriver methods should be optional (?) and delegate to FileSystemGlobalState in FileSystemActorDriver.
- The isolate-side actorDriver object lives in dynamic-isolate-runtime/src/index.cts (not src/dynamic/host-runtime.ts). Its DynamicActorDriver is a locally-typed subset of ActorDriver.
- drizzle sqlite-proxy expects positional arrays from the callback; RawDatabaseClient returns objects. Use Object.values() to convert. Override checks go at top of createClient in db/drizzle/mod.ts.
- Driver test suite is entirely broken by pre-existing esbuild error in engine/actor-driver.ts (#hwsAckState private field). Cannot run any driver-file-system tests. This is NOT caused by dynamic actor changes.
- Fixture actors in actors/ directory (dbActorRaw.ts, dbActorDrizzle.ts) are already loaded as both static and dynamic actors via registry-loader.ts. No need to create new fixture actors for SQLite proxy testing.
- Dynamic SQLite proxy tests should use describe.skipIf(!driverTestConfig.isDynamic) to only run when the dynamic registry variant is active.
- Tests depending on optional peer deps (@secure-exec/typescript, secure-exec) must use describe.skipIf with require.resolve to skip gracefully.
- GlobalActorOptionsInput is a z.input<> type. Cannot use `interface extends` with it. Use intersection type instead.
- Dynamic runtime status lives in src/dynamic/runtime-status.ts. Use standalone transition functions (transitionToStarting, etc.) rather than class methods for cross-driver compatibility.
- coalesceDynamicStartup in startup-coalescing.ts orchestrates dynamic actor startup with coalescing (concurrent requests join existing promise) and generation tracking (stale completions discarded). Integrate it into driver startActor paths.
- When moving code into closures, capture narrowed TypeScript variables into local constants first to avoid losing type narrowing.

## 2026-03-20 - US-001
- What was implemented:
  - Added sqliteExec and sqliteBatch keys to DYNAMIC_HOST_BRIDGE_GLOBAL_KEYS in runtime-bridge.ts
  - Added #actorAppDatabases map, #appDbsDir directory, and app-databases directory creation to FileSystemGlobalState
  - Added #getOrCreateActorAppDatabase(actorId) with WAL mode, #closeActorAppDatabase(actorId)
  - Added sqliteExec(actorId, sql, params) and sqliteBatch(actorId, statements) methods
  - Extended sleepActor and destroyActor to close and delete app databases
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/dynamic/runtime-bridge.ts
  - rivetkit-typescript/packages/rivetkit/src/drivers/file-system/global-state.ts
- **Learnings for future iterations:**
  - SqliteRuntimeDatabase is an abstraction over bun:sqlite, node:sqlite, and better-sqlite3. It normalizes to exec/run/get/all/close. No prepare/columns available.
  - For column-oriented results, use all() + Object.keys() on first row.
  - App databases are stored at <storagePath>/app-databases/<actorId>.db, separate from KV databases at <storagePath>/databases/<actorId>.db.
  - destroyActor deletes files in parallel via Promise.all with individual try/catch blocks ignoring ENOENT.
---

## 2026-03-20 - US-002
- What was implemented:
  - Added sqliteExec and sqliteBatch as optional methods on ActorDriver interface
  - Added delegation methods in FileSystemActorDriver that call through to FileSystemGlobalState
  - Added sqliteExecRef and sqliteBatchRef in #setIsolateBridge() using the same makeRef/makeExternalCopy pattern as KV callbacks
  - SQLite bridge refs JSON-serialize params from the isolate and return JSON-serialized results via makeExternalCopy
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/actor/driver.ts
  - rivetkit-typescript/packages/rivetkit/src/drivers/file-system/actor.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/isolate-runtime.ts
- **Learnings for future iterations:**
  - There is no separate host-runtime.ts file. Both isolated-vm and secure-exec runtimes use the same #setIsolateBridge() in isolate-runtime.ts because secure-exec wraps isolated-vm.
  - Bridge callbacks for synchronous host operations (like SQLite) use the same makeRef pattern as async KV callbacks.
  - JSON serialization is used for SQLite params/results crossing the isolate boundary (params as JSON string, results as JSON string via makeExternalCopy).
  - FileSystemActorDriver delegates to FileSystemGlobalState for all operations (KV, alarms, SQLite). New ActorDriver methods should follow this pattern.
  - The startSleep duplicate identifier error in file-system/actor.ts is pre-existing (property declaration + method definition conflict).
---

## 2026-03-20 - US-003
- What was implemented:
  - Added sqliteExec and sqliteBatch as optional refs on DynamicHostBridge interface in the isolate bootstrap
  - Read them via getOptionalHostRef in readHostBridge()
  - Added overrideRawDatabaseClient and overrideDrizzleDatabaseClient to DynamicActorDriver interface
  - Implemented overrideRawDatabaseClient on the actorDriver object: returns a RawDatabaseClient whose exec() calls bridgeCall to sqliteExec, JSON-serializes params, parses the column-oriented result, and maps rows to objects
  - overrideDrizzleDatabaseClient returns undefined to let the raw override handle it
- Files changed:
  - rivetkit-typescript/packages/rivetkit/dynamic-isolate-runtime/src/index.cts
- **Learnings for future iterations:**
  - The isolate-side actorDriver is in dynamic-isolate-runtime/src/index.cts, not in src/dynamic/. The PRD refers to host-runtime.ts but the actual file is index.cts.
  - The DynamicActorDriver interface in the bootstrap is a locally-defined subset of ActorDriver. It already has pre-existing TS2345 errors (missing kvDeleteRange, kvListRange).
  - SQLite bridge refs on the isolate side should be optional (getOptionalHostRef) since not all drivers may support SQLite.
  - bridgeCall handles ExternalCopy.copy() automatically, so JSON.parse on the result works for both sync and async bridge returns.
---

## 2026-03-20 - US-004
- What was implemented:
  - Added drizzle override check at top of createClient() in db/drizzle/mod.ts
  - Added raw database override check that builds a drizzle sqlite-proxy on top of RawDatabaseClient
  - sqlite-proxy callback handles 'run' (exec + return empty), 'get' (first row as array), and 'all' (all rows as arrays) methods
  - Added createRawOverrideProxyCallback helper that converts RawDatabaseClient objects to positional arrays for drizzle's mapResultRow
  - Added runInlineMigrationsViaRawClient helper for running drizzle migrations through a RawDatabaseClient
  - Updated onMigrate to use rawOverrideClient when available, falling back to waDbInstance
  - Existing KV-backed path remains unchanged as fallback
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/db/drizzle/mod.ts
- **Learnings for future iterations:**
  - DatabaseProviderContext already has overrideRawDatabaseClient and overrideDrizzleDatabaseClient as optional callbacks (defined in db/config.ts).
  - RawDatabaseClient.exec returns Record<string, unknown>[] (objects keyed by column name), but drizzle's sqlite-proxy mapResultRow expects positional arrays. Use Object.values(row) to convert.
  - The drizzle override path currently always returns undefined from the isolate side, so it's a stub path. The raw override is the active path for dynamic actors.
  - runInlineMigrations uses @rivetkit/sqlite Database directly (waDb.exec with callback). For override paths, a separate migration runner using RawDatabaseClient.exec is needed.
---

## 2026-03-20 - US-005
- What was implemented:
  - Created new driver test file src/driver-test-suite/tests/dynamic-sqlite-proxy.ts with tests for raw db() and drizzle db() through the SQLite proxy bridge
  - Tests cover: CRUD operations, sleep/wake persistence, transactions, drizzle migrations, and multi-statement inserts
  - All tests are gated with describe.skipIf(!driverTestConfig.isDynamic) so they only run for the dynamic registry variant
  - Wired the new test runner into src/driver-test-suite/mod.ts
  - Existing fixture actors (dbActorRaw, dbActorDrizzle) in actors/ directory are reused since registry-loader.ts already loads them as dynamic actors
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/driver-test-suite/tests/dynamic-sqlite-proxy.ts (new)
  - rivetkit-typescript/packages/rivetkit/src/driver-test-suite/mod.ts
- **Learnings for future iterations:**
  - The entire driver test suite is blocked by a pre-existing esbuild error in engine/actor-driver.ts (#hwsAckState private field referenced before declaration). No driver tests can run until this is fixed.
  - registry-loader.ts automatically creates dynamic actor wrappers for ALL actors in fixtures/driver-test-suite/actors/. New fixture actors placed in that directory are automatically available as both static and dynamic actors.
  - The driver-file-system.test.ts runs both "static" and "dynamic" registry variants via getDriverRegistryVariants(). Dynamic variant requires secure-exec dist to be installed.
  - New driver test functions follow the pattern: export function runXxxTests(driverTestConfig: DriverTestConfig) with describe.skipIf for conditional skipping.
---

## 2026-03-20 - US-006
- What was implemented:
  - Replaced pre-release commit hash URL (pkg.pr.new/rivet-dev/secure-exec@7659aba) with secure-exec@0.1.0 in examples/ai-generated-actor/package.json
  - Removed stale sandboxed-node references from isolate-runtime.ts (both npm package specifier fallback and local dist path fallback)
  - Added secure-exec and @secure-exec/typescript as optional peer dependencies of rivetkit
  - Updated pnpm-lock.yaml with the new resolved version
- Files changed:
  - examples/ai-generated-actor/package.json
  - rivetkit-typescript/packages/rivetkit/package.json
  - rivetkit-typescript/packages/rivetkit/src/dynamic/isolate-runtime.ts
  - pnpm-lock.yaml
- **Learnings for future iterations:**
  - sandboxed-node was the old package name before secure-exec was published. It does not exist on npm and the monorepo no longer has a sandboxed-node package directory.
  - The secure-exec monorepo at ~/secure-exec-rivet/packages/ contains: secure-exec, secure-exec-typescript, playground, reserve, website.
  - The ai-generated-actor example has pre-existing typecheck errors because it depends on built rivetkit types (.d.ts files). Run `pnpm build -F rivetkit` first to resolve.
  - The resolveSecureExecEntryPath() function in isolate-runtime.ts has three resolution tiers: explicit env var, npm package resolution, local dist fallback.
---

## 2026-03-20 - US-007
- What was implemented:
  - Created src/dynamic/compile.ts with compileActorSource function
  - Dynamically loads @secure-exec/typescript and secure-exec using the build-specifier-from-parts pattern (["@secure-exec", "typescript"].join("/") and ["secure", "exec"].join("-"))
  - Both modules are cached via promise singletons (same pattern as isolate-runtime.ts)
  - Loads secure-exec's createNodeDriver and createNodeRuntimeDriverFactory for the compiler isolate drivers
  - Calls createTypeScriptTools() then tools.compileSource() with user source and compiler options
  - When typecheck is false, sets compilerOptions.noCheck = true for fast type-stripping
  - Maps SourceCompileResult to CompileActorSourceResult with js, sourceMap, success, and diagnostics fields
  - Sets default module format to ESM (esnext) with bundler moduleResolution, or commonjs when format is "commonjs"
  - Exported compileActorSource, CompileActorSourceOptions, CompileActorSourceResult, and TypeScriptDiagnostic from src/dynamic/mod.ts
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/dynamic/compile.ts (new)
  - rivetkit-typescript/packages/rivetkit/src/dynamic/mod.ts
- **Learnings for future iterations:**
  - @secure-exec/typescript exports createTypeScriptTools which takes { systemDriver, runtimeDriverFactory, memoryLimit?, cpuTimeLimitMs? } and returns CompilerTools with compileSource/typecheckSource methods.
  - secure-exec exports createNodeDriver and createNodeRuntimeDriverFactory directly (re-exported from @secure-exec/node).
  - SourceCompileResult has outputText (not js) and sourceMapText (not sourceMap). Must map field names.
  - The resolveEsmPackageEntry pattern from isolate-runtime.ts is duplicated in compile.ts because compile.ts is a separate module that can be imported independently.
  - nativeDynamicImport pattern (try direct import, fallback to Function("return import(...)")) is needed to handle Vite SSR and VM-backed test runners.
---

## 2026-03-20 - US-008
- What was implemented:
  - Extended DynamicSourceFormat in runtime-bridge.ts with 'esm-ts' and 'commonjs-ts' variants
  - Updated normalizeLoadResult in isolate-runtime.ts to accept TS source formats
  - Updated materializeDynamicSource in isolate-runtime.ts with new 'esm-ts'/'commonjs-ts' cases that call compileActorSource before writing to the sandbox filesystem
  - TS compilation uses typecheck: false for fast type-stripping (consistent with runtime convenience use case)
  - If compilation fails, throws an error with diagnostic messages
  - Exports from dynamic/mod.ts were already set up in US-007 (compileActorSource, types)
  - Added unit tests in compile.test.ts: valid TS, type errors with typecheck: true, and type-stripping with typecheck: false
  - Tests use describe.skipIf to gracefully skip when @secure-exec/typescript is not installed
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/dynamic/runtime-bridge.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/isolate-runtime.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/compile.test.ts (new)
- **Learnings for future iterations:**
  - @secure-exec/typescript is an optional peer dependency. Tests that depend on it must skip gracefully using describe.skipIf with a require.resolve check.
  - TS source formats ('esm-ts', 'commonjs-ts') are compiled to their JS equivalents ('esm-js', 'commonjs-js') during materializeDynamicSource, before the source reaches the sandbox filesystem.
  - The compiled JS format is passed through to the bootstrap config, so the isolate sees plain JS regardless of the original source format.
---

## 2026-03-20 - US-009
- What was implemented:
  - Added DynamicStartupFailed error subclass (group: "dynamic", code: "dynamic_startup_failed", statusCode: 503) to actor/errors.ts
  - Added DynamicLoadTimeout error subclass (group: "dynamic", code: "dynamic_load_timeout", statusCode: 503) to actor/errors.ts
  - Added DynamicStartupOptions interface with timeoutMs, retryInitialDelayMs, retryMaxDelayMs, retryMultiplier, retryJitter, maxAttempts
  - Added DYNAMIC_STARTUP_DEFAULTS constant with all default values
  - Added DynamicActorOptions type (intersection of GlobalActorOptionsInput & { startup? })
  - Added DynamicActorReloadContext class with request field, following DynamicActorAuthContext pattern
  - Added DynamicActorCanReload callback type
  - Updated DynamicActorConfigInput with canReload and options fields
  - Updated DynamicActorDefinition to store canReload callback and resolved startup options
  - Exported all new types from dynamic/mod.ts
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/actor/errors.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/internal.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/mod.ts
- **Learnings for future iterations:**
  - GlobalActorOptionsInput is a z.input<> Zod type. You cannot use `interface extends` with it (TS2312). Use intersection type (`type X = GlobalActorOptionsInput & { ... }`) instead.
  - DynamicActorDefinition resolves startup options in the constructor by merging defaults with user-provided values, so consumers always get Required<DynamicStartupOptions>.
  - DynamicActorReloadContext follows the same pattern as DynamicActorAuthContext (extends DynamicActorContextBase, adds request field).
  - Error subclasses use statusCode 503 (Service Unavailable) for dynamic startup failures since the actor is temporarily unable to handle requests.
---

## 2026-03-20 - US-010
- What was implemented:
  - Created src/dynamic/runtime-status.ts with the shared host-side dynamic runtime status model
  - Defined DynamicRuntimeState union type with "inactive", "starting", "running", "failed_start" states
  - Defined DynamicRuntimeStatus interface with all metadata fields: error tracking (lastStartErrorCode, lastStartErrorMessage, lastStartErrorDetails, lastFailureAt), backoff state (retryAt, retryAttempt), reload rate-limit tracking (reloadCount, reloadWindowStart), generation counter, and startupPromise
  - Implemented createDynamicRuntimeStatus() factory returning inactive state
  - Implemented transitionToStarting() that synchronously increments generation and creates startupPromise via promiseWithResolvers
  - Implemented transitionToRunning() that resolves startupPromise and resets retry state
  - Implemented transitionToFailedStart() that records error metadata, increments retryAttempt, and rejects startupPromise
  - Implemented transitionToInactive() that clears all metadata (except generation which only increments)
  - Exported all types and functions from dynamic/mod.ts
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/dynamic/runtime-status.ts (new)
  - rivetkit-typescript/packages/rivetkit/src/dynamic/mod.ts
- **Learnings for future iterations:**
  - The status model uses mutation-based transitions (modifying the object in place) rather than creating new objects, matching the pattern used for ActorEntry in global-state.ts.
  - generation is never reset, even when transitioning to inactive. This ensures monotonicity across the actor's lifetime so stale async completions are always detectable.
  - startupPromise uses the codebase's promiseWithResolvers utility which requires an onReject callback to handle unhandled rejections.
  - The transition functions are standalone exports (not methods on a class) so they can be used by both file-system and engine drivers without inheritance.
---

## 2026-03-20 - US-011
- What was implemented:
  - Created src/dynamic/startup-coalescing.ts with coalesceDynamicStartup orchestration function
  - coalesceDynamicStartup handles: running (no-op), starting (coalesce via existing promise), inactive/expired failed_start (new attempt with generation tracking)
  - Captures generation synchronously before async work; discards stale completions if generation changed
  - On success: transitionToRunning. On failure: transitionToFailedStart with exponential backoff metadata
  - Added computeBackoffDelay helper (min(maxDelay, initialDelay * multiplier^attempt) with optional jitter)
  - Integrated into FileSystemGlobalState.startActor: dynamic actors now use coalesceDynamicStartup to manage startup lifecycle
  - Added #dynamicStatuses map to FileSystemGlobalState for per-actor DynamicRuntimeStatus tracking
  - Added #getOrCreateDynamicStatus and getDynamicStatus helpers
  - Cleanup: sleepActor and destroyActor now transition status to inactive and delete from map
  - Exported coalesceDynamicStartup and computeBackoffDelay from dynamic/mod.ts
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/dynamic/startup-coalescing.ts (new)
  - rivetkit-typescript/packages/rivetkit/src/dynamic/mod.ts
  - rivetkit-typescript/packages/rivetkit/src/drivers/file-system/global-state.ts
- **Learnings for future iterations:**
  - When moving code into an async closure for coalesceDynamicStartup, TypeScript loses narrowing on outer variables like entry.state. Capture narrowed values into local constants before the closure.
  - The two coalescing mechanisms (ActorEntry.startPromise and DynamicRuntimeStatus.startupPromise) complement each other: the former handles general actor-start coalescing, the latter handles dynamic-specific failed-start lifecycle.
  - coalesceDynamicStartup re-throws startup errors after recording them in the status model, allowing the caller (startActor) to also handle cleanup like disposing the runtime.
  - DynamicActorDefinition.startupOptions provides resolved Required<DynamicStartupOptions> with defaults already merged.
---

## 2026-03-20 - US-012
- What was implemented:
  - Added `signal: AbortSignal` to `DynamicActorLoaderContext` so user-provided loaders can check cancellation
  - Updated `createDynamicActorLoaderContext` to accept and pass through signal
  - Added `signal?: AbortSignal` to `DynamicActorIsolateRuntimeConfig`
  - Updated `DynamicActorIsolateRuntime.start()` to create a default signal and pass it to the loader context
  - Updated `coalesceDynamicStartup` to create an AbortController per startup attempt, set up a setTimeout for `timeoutMs`, and abort with `DynamicLoadTimeout` on expiry
  - The startupFn signature now accepts `(signal: AbortSignal)` and callers thread it through to the runtime config
  - Timeout errors are caught and converted to DynamicLoadTimeout, which participates in normal backoff via `transitionToFailedStart`
  - Updated FileSystemGlobalState startActor closure to accept and pass signal
- Files changed:
  - rivetkit-typescript/packages/rivetkit/src/dynamic/internal.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/isolate-runtime.ts
  - rivetkit-typescript/packages/rivetkit/src/dynamic/startup-coalescing.ts
  - rivetkit-typescript/packages/rivetkit/src/drivers/file-system/global-state.ts
- **Learnings for future iterations:**
  - coalesceDynamicStartup's startupFn signature is `(signal: AbortSignal) => Promise<void>`. All callers must accept and thread the signal.
  - The AbortController is created inside coalesceDynamicStartup, not by the caller. This centralizes timeout management.
  - When the abort reason is a DynamicLoadTimeout instance, the catch block uses it as the effective error instead of the raw AbortError. This keeps error codes consistent in the backoff state.
  - DynamicActorLoaderContext.signal is always present. When no signal is provided to the runtime config, a fallback `AbortSignal.timeout(MAX_SAFE_INTEGER)` is used so the loader always has a valid signal.
---
