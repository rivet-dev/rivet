Build a minimal static HTML app.

Requirements:
- The app must run with `npm run dev` on port 5173
- Use a `package.json` dev script that runs `python3 -m http.server 5173`
- Do not add dependencies or run package installation for this eval
- The home page must load at `/`
- The page must contain a visible `<h1>` with the exact text `Hello from Eval`
- The page must contain a visible paragraph with the exact text `basic smoke test`
- Keep the implementation minimal
