---
import BaseLayout from './BaseLayout.astro';
import { Header } from '@/components/v2/Header';

interface Props {
	title: string;
	description?: string;
	canonicalUrl?: string;
	ogImage?: string;
	keywords?: string[];
}

const { title, description, canonicalUrl, ogImage, keywords } = Astro.props;
---

<BaseLayout title={title} description={description} canonicalUrl={canonicalUrl} ogImage={ogImage} keywords={keywords}>
	<Header variant="floating" client:load />
	<slot />
</BaseLayout>
