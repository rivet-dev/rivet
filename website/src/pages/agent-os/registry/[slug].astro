---
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import { Icon } from '@rivet-gg/icons';
import { registry } from '@/data/registry';

export function getStaticPaths() {
	return registry.map((entry) => ({
		params: { slug: entry.slug },
		props: { entry },
	}));
}

const { entry } = Astro.props;

const TYPE_LABELS: Record<string, string> = {
	"file-system": "File System",
	tool: "Tool",
	agent: "Agent",
	"sandbox-extension": "Sandbox Extension",
};

const TYPE_COLORS: Record<string, string> = {
	"file-system": "bg-blue-500/20 text-blue-400 border-blue-500/30",
	tool: "bg-emerald-500/20 text-emerald-400 border-emerald-500/30",
	agent: "bg-orange-500/20 text-orange-400 border-orange-500/30",
	"sandbox-extension": "bg-purple-500/20 text-purple-400 border-purple-500/30",
};

const isComingSoon = entry.status === "coming-soon";
const installCmd = !isComingSoon ? `npm install ${entry.package}` : null;
---

<MarketingLayout
	title={entry.title}
	description={entry.description}
	canonicalUrl={`https://rivet.dev/agent-os/registry/${entry.slug}/`}
>
	<main class="min-h-screen w-full bg-black">
		<div class="relative isolate overflow-hidden pt-40">
			<div class="mx-auto max-w-3xl px-6 lg:px-8">
				<a
					href="/agent-os/registry"
					class="inline-flex items-center text-sm text-white/50 hover:text-white mb-8 transition-colors no-underline"
				>
					&larr; Back to Registry
				</a>

				<div class="flex items-center gap-4 mb-4">
					{entry.image && (
						<img
							src={entry.image}
							alt={entry.title}
							width={40}
							height={40}
							class="object-contain"
						/>
					)}
					{entry.icon && (
						<Icon icon={entry.icon} className="text-white w-10 h-10" />
					)}
					<h1 class="text-5xl font-700 text-white leading-[1.1] tracking-normal">
						{entry.title}
					</h1>
					{isComingSoon && (
						<span class="text-xs px-2.5 py-1 rounded-full border border-white/20 text-white/50">
							Coming Soon
						</span>
					)}
				</div>

				<div class="flex gap-2 mt-4">
					{entry.types.map((type: string) => (
						<span
							class={`text-xs px-2 py-0.5 rounded-full border ${TYPE_COLORS[type]}`}
						>
							{TYPE_LABELS[type]}
						</span>
					))}
				</div>

				<p class="mt-6 text-lg text-white/70 leading-relaxed">
					{entry.description}
				</p>
			</div>
		</div>

		<div class="mx-auto max-w-3xl px-6 lg:px-8 pt-10 pb-24">
			{isComingSoon ? (
				<div class="rounded-xl border border-white/10 bg-white/2 p-8 text-center">
					<p class="text-white/50 text-lg">This integration is coming soon.</p>
				</div>
			) : (
				<>
					<section>
						<h2 class="text-xl font-600 text-white mb-3">Install</h2>
						<div class="flex items-center gap-2 bg-[#111] rounded-lg border border-white/10 px-4 py-3 mb-6">
							<code class="text-sm text-white/90 flex-1 font-mono">
								{installCmd}
							</code>
						</div>
					</section>

					<a
						href={`https://www.npmjs.com/package/${entry.package}`}
						target="_blank"
						rel="noopener noreferrer"
						class="inline-flex items-center justify-center rounded-lg bg-white/10 hover:bg-white/15 border border-white/10 text-white text-sm font-medium px-4 py-2 transition-colors no-underline"
					>
						View on npm
					</a>
				</>
			)}
		</div>
	</main>
</MarketingLayout>
