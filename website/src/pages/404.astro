---
import { getImage } from 'astro:assets';
import MarketingLayout from '@/layouts/MarketingLayout.astro';
import { NotFoundPage } from '@/components/marketing/NotFoundPage';
const imgThinker = { src: "https://assets.rivet.dev/website/images/thinking/thinker.jpg", width: 3140, height: 4000, format: "jpg" };
const optimizedImage = await getImage({
  src: imgThinker,
  width: 800,
  format: 'webp',
  quality: 80
});
---

<MarketingLayout
  title="Page Not Found"
  description="The page you're looking for doesn't exist."
  robots="noindex, nofollow"
>
  <div class="min-h-screen font-sans text-ink-soft">
    <NotFoundPage thinkingImage={optimizedImage.src} client:load />
  </div>
</MarketingLayout>
