/** @type {import('next-sitemap').IConfig} */
module.exports = {
	siteUrl: "https://www.rivet.dev",
	outDir: "out/",
	// TODO: We should pull the modification data from blog posts automatically
	autoLastmod: false,
};
